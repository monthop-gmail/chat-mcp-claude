version: '3.8'

services:
  chat-mcp:
    build: .
    container_name: chat-mcp-claude
    restart: unless-stopped
    volumes:
      - ./db:/app/db:ro
      - ./data:/app/data
    environment:
      - CHAT_DB_PATH=/app/data/chat.db
      - CHAT_DIR=/app/db
    stdin_open: true
    tty: true
